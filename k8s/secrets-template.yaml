# secrets-template.yaml
# TEMPLATE ONLY — never commit real values.
# Copy to secrets.yaml (gitignored), fill in values, then:
#   kubectl apply -f k8s/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: swarm-secrets
  namespace: children-of-israel
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_WITH_REAL_KEY"
  ANTHROPIC_API_KEY: "REPLACE_WITH_REAL_KEY"
  POSTGRES_URL: "postgresql://swarm:REPLACE_PASSWORD@postgres-service:5432/swarm"
  REDIS_URL: "redis://redis-service:6379/0"
