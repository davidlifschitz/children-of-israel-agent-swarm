apiVersion: v1
kind: ConfigMap
metadata:
  name: swarm-config
  namespace: children-of-israel
data:
  mission.yaml: |
    # Mounted from config/mission.yaml — edit values here for production
    sla:
      moses: 300
      tier_1: 60
      tier_2: 45
      tier_3: 30
      tier_4: 20
    checkpointing:
      backend: "postgres"
      postgres_url: null
      redis_url: null
